---
title: GitHub Integration
description: "How to connect your Same project to GitHub."
---

Manage your project's code using Git and GitHub directly within Same.

## Option 1: Using Same Agent

Instruct Same to perform Git actions.

```md
Connect this project to a new public GitHub repository named 'my-same-project'.
Commit all changes with message 'Initial commit' and push to main.
```

<Tip>
  You may be prompted in the terminal to enter your GitHub username and a
  [Personal Access
  Token](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)
  (use the token as the password).
</Tip>

<Frame caption="Instruct Same to run Git commands.">
  <img
    src="/images/agent-github.png"
    alt="Screenshot of Same running Git commands."
  />
</Frame>

## Option 2: Using the Integrated Terminal

Use the standard Git CLI for more control.

<Steps>
  <Step title="Open the Terminal">
    Click the arrows icon to open the Workbench, then select the Terminal tab.
  </Step>
  <Step title="Navigate to Project Directory">
    **Important:** The terminal starts in `/home/project`. Your code is in a subdirectory.
    1. Run `ls` to find your project's directory name.
    2. Run `cd [your-project-directory-name]` to enter it.
  </Step>
  <Step title="Run Git Commands">
    Execute standard Git commands:
    ```bash
    git init
    git remote add origin <your-repo-url>
    git add .
    git commit -m "Your commit message"
    git push origin main
    # etc. (git pull, git status, git log)
    ```
  </Step>
</Steps>

<Info>
  The terminal resets on page refresh. You may need to run `git init` again. We
  are improving this experience.
</Info>

<Frame caption="Run standard Git commands in the terminal after navigating to the project directory.">
  <img
    src="/images/terminal-github.png"
    alt="Screenshot of the integrated terminal showing Git commands being run."
  />
</Frame>

<Warning>
  **Always** run `cd [your-project-directory-name]` before Git commands like
  `add`, `commit`, or `push`. Running them in the wrong directory can cause
  errors.
</Warning>

## Downloading Your Code

Download your project as a ZIP file.

1.  Open the **Workbench**.
2.  Click the **Download icon** in the Workbench header.

<Frame caption="Download your project code as a ZIP file.">
  <img
    src="/images/download-code.png"
    alt="Screenshot showing the download icon in the Workbench header."
  />
</Frame>
